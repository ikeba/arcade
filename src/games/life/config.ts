export const GRID_COLS = 36;
export const GRID_ROWS = 25;

// Milliseconds between generations.
export const TICK_MS = 200;

// Grid line opacity (0..1). Low so the grid reads as a backdrop, not a cage.
export const GRID_ALPHA = 0.1;

// Pixels of gap between the grid and its surrounding elements (canvas, title, rules, restart).
export const GAP = 16;
